%SMALLVALUES = findNSmallest(INPUTVALUES, N)
%
% Finds the N smallest values in INPUTVALUES and outputs them to SMALLVALUES
%
%[SMALLVALUES, INDEX] = findNSmallest(INPUTVALUES, N)
%
% INDEX is the index of the returned values in the original INPUTVALUES 